project:
  machine_name: drupalcon-seattle
  prefix: SEATTLE
  human_name: 'DrupalCon Seattle Cloud CD Demo'
  profile:
    name: demo_umami
  local:
    protocol: http
    hostname: 'lc.${project.machine_name}'
git:
  default_branch: drupalcon-seattle
  remotes:
    - cddemo11@svn-28441.prod.hosting.acquia.com:cddemo11.git
  hooks:
    pre-commit: false
    commit-msg: false
deploy:
  tag_source: true
drush:
  aliases:
    remote: '${project.machine_name}.dev'
    local: self
    ci: self
  default_alias: '${drush.aliases.local}'
modules:
  local:
    enable: [dblog, devel, seckit, views_ui]
    uninstall: [acquia_connector, shield]
  ci:
    enable: {  }
    uninstall: [acquia_connector, shield]
  dev:
    enable: [acquia_connector, shield]
    uninstall: {  }
  test:
    enable: [acquia_connector, shield]
    uninstall: [devel, views_ui]
  prod:
    enable: [acquia_connector, shield]
    uninstall: [devel, views_ui]
